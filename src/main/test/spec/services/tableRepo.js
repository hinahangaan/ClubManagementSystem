'use strict';

describe('Service: tableRepo', function () {

  // load the service's module
  beforeEach(module('cmsAppDev'));

  // instantiate service
  var tableRepo;
  beforeEach(inject(function (_tableRepo_) {
    tableRepo = _tableRepo_;
  }));


});
